# =========================================================================
# VARIÁVEIS DE AMBIENTE - GOALMANAGER
# =========================================================================
# Arquivo: .env.example
# Descrição: Template de configuração para ambiente Docker
# Instruções: Copie este arquivo para .env e ajuste os valores
# =========================================================================

# =========================================================================
# CONFIGURAÇÕES GERAIS
# =========================================================================
# Timezone do sistema (ajustar conforme localização)
TZ=America/Sao_Paulo

# Ambiente (development, staging, production)
NODE_ENV=development

# =========================================================================
# CONFIGURAÇÕES DO MYSQL
# =========================================================================
# Senha do usuário root (MUDE EM PRODUÇÃO!)
MYSQL_ROOT_PASSWORD=root123!@#

# Nome do banco de dados
MYSQL_DATABASE=goalmanager

# Usuário da aplicação
MYSQL_USER=goalmanager_user

# Senha do usuário da aplicação (MUDE EM PRODUÇÃO!)
MYSQL_PASSWORD=goalmanager_pass123

# Porta do MySQL (padrão: 3306)
MYSQL_PORT=3306

# =========================================================================
# CONFIGURAÇÕES DO PHPMYADMIN
# =========================================================================
# Porta do phpMyAdmin (padrão: 8080)
PHPMYADMIN_PORT=8080

# URL completa do phpMyAdmin (para reverse proxy)
PHPMYADMIN_URL=http://localhost:8080

# =========================================================================
# CONFIGURAÇÕES DO ADMINER
# =========================================================================
# Porta do Adminer (padrão: 8081)
ADMINER_PORT=8081



# =========================================================================
# CONFIGURAÇÕES DA APLICAÇÃO (para futuro backend)
# =========================================================================
# Porta da API
API_PORT=3000

# URL base da API
API_BASE_URL=http://localhost:3000

# Chave secreta para JWT (GERE UMA NOVA EM PRODUÇÃO!)
JWT_SECRET=goalmanager_super_secret_key_change_in_production_123456789

# Duração do token de acesso (em segundos)
JWT_ACCESS_EXPIRATION=3600

# Duração do token de refresh (em segundos)
JWT_REFRESH_EXPIRATION=604800

# =========================================================================
# CONFIGURAÇÕES DE EMAIL (para futuras notificações)
# =========================================================================
# Provedor de email (smtp, sendgrid, mailgun, etc.)
EMAIL_PROVIDER=smtp

# Configurações SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=seu-email@gmail.com
SMTP_PASSWORD=sua-senha-app

# Email remetente
EMAIL_FROM=goalmanager@exemplo.com
EMAIL_FROM_NAME=GoalManager

# =========================================================================
# CONFIGURAÇÕES DE STORAGE (para futuro upload de arquivos)
# =========================================================================
# Tipo de storage (local, s3, gcs, etc.)
STORAGE_TYPE=local

# Diretório local para uploads
STORAGE_LOCAL_PATH=./uploads

# Configurações AWS S3 (se usar)
# AWS_ACCESS_KEY_ID=sua_access_key
# AWS_SECRET_ACCESS_KEY=sua_secret_key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=goalmanager-uploads

# =========================================================================
# CONFIGURAÇÕES DE LOGS
# =========================================================================
# Nível de log (debug, info, warn, error)
LOG_LEVEL=info

# Formato de log (text, json)
LOG_FORMAT=json

# =========================================================================
# CONFIGURAÇÕES DE RATE LIMITING
# =========================================================================
# Número máximo de requisições por minuto
RATE_LIMIT_MAX=1000

# Janela de tempo em milissegundos
RATE_LIMIT_WINDOW=60000

# =========================================================================
# CONFIGURAÇÕES DE CORS
# =========================================================================
# Origens permitidas (separadas por vírgula)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:4173

# =========================================================================
# CONFIGURAÇÕES DE MONITORAMENTO
# =========================================================================
# Habilitar métricas Prometheus
ENABLE_METRICS=false

# Porta para métricas
METRICS_PORT=9090

# =========================================================================
# OBSERVAÇÕES IMPORTANTES
# =========================================================================
#
# 1. SEGURANÇA:
#    - Sempre altere as senhas padrão em produção
#    - Use senhas fortes com pelo menos 12 caracteres
#    - Gere uma nova JWT_SECRET usando: openssl rand -hex 32
#
# 2. PERFORMANCE:
#    - Ajuste os recursos do MySQL conforme sua máquina
#    - Configure cache Redis adequadamente
#    - Use CDN para assets estáticos em produção
#
# 3. BACKUP:
#    - Configure backup automático do MySQL
#    - Use volumes Docker nomeados para persistência
#    - Teste restore regularmente
#
# 4. MONITORAMENTO:
#    - Configure logs centralizados
#    - Use health checks
#    - Configure alertas para falhas
#
# =========================================================================